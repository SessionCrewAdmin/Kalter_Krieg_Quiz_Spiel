(()=>{const KEY='kathleenClassListsV1';function uid(){return 'class-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,6)}function cleanStudents(v){const a=Array.isArray(v)?v:String(v||'').split(/[\n,;]+/);const seen=new Set(),out=[];for(const raw of a){const n=String(raw).trim().replace(/\s+/g,' ');if(!n)continue;const k=n.toLocaleLowerCase('de');if(seen.has(k))continue;seen.add(k);out.push(n)}return out}function load(){try{const v=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(v)?v.filter(x=>x&&x.id&&x.name&&Array.isArray(x.students)):[]}catch(e){return[]}}function save(lists){localStorage.setItem(KEY,JSON.stringify(lists));window.dispatchEvent(new CustomEvent('kathleen:classlists'))}function upsert(data){const lists=load(),id=data.id||uid(),item={id,name:String(data.name||'').trim(),students:cleanStudents(data.students),updatedAt:new Date().toISOString()};if(!item.name)throw new Error('Klassenname fehlt');const i=lists.findIndex(x=>x.id===id);if(i>=0)lists[i]=item;else lists.push(item);lists.sort((a,b)=>a.name.localeCompare(b.name,'de',{numeric:true}));save(lists);return item}function remove(id){save(load().filter(x=>x.id!==id))}function get(id){return load().find(x=>x.id===id)||null}function shuffle(input){const a=[...input];for(let i=a.length-1;i>0;i--){let j;if(window.crypto&&crypto.getRandomValues){const u=new Uint32Array(1);crypto.getRandomValues(u);j=u[0]%(i+1)}else j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}window.KathleenClassLists={load,save,upsert,remove,get,cleanStudents,shuffle,key:KEY}})();